# 로컬 부하 실험의 telemetry만 수신하는 격리 broker 설정입니다.
listener 1883 0.0.0.0
allow_anonymous true
persistence false

# 수동 ACK subscriber를 1,000건 단위로 burst하는 실험에서 broker 기본 queue(1,000)가
# backend보다 먼저 측정 상한이 되지 않도록 staging에서만 명시적으로 늘립니다.
max_queued_messages 20000
